#include <iostream>

using namespace std;
const int N=100000+9;
int a[N];
int main()
{
   int n,counter=0;
   cin>>n;
   for(int i=0;i<n;++i){
    cin>>a[i];
    if(i==0||a[i-1]!=a[i])counter++;
   }
   cout<<counter;
return 0;
}

